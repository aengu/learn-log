[pytest]
DJANGO_SETTINGS_MODULE = config.settings
pythonpath = .

markers =
    healthcheck: 실제 LLM API를 호출한다. 기본 실행에서 제외되고 healthcheck.yml에서만 돈다.
# 기본 pytest 실행에 실제 API 호출이 섞이면 외부 장애로 CI가 빨개진다.
addopts = -m "not healthcheck"
